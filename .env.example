# ── Variables de entorno del CRM de Ocean (configurar en Vercel) ──

# Supabase del CRM + Valeria (la service_role SOLO vive aquí en el backend)
SUPABASE_URL=https://jsohtuinuvbnsqdnjhsc.supabase.co
SUPABASE_SERVICE_KEY=tu_service_role_key

# Seguridad JWT (genera uno con: openssl rand -base64 48)
JWT_SECRET=cadena_aleatoria_larga

# Anthropic — para la vista de Inteligencia y el chat con IA del CRM
ANTHROPIC_API_KEY=tu_anthropic_api_key

# Evolution API — para enviar WhatsApp desde el CRM (WhatsApp Web propio)
EVOLUTION_URL=http://178.156.183.173
EVOLUTION_API_KEY=tu_evolution_api_key
EVOLUTION_INSTANCE=ocean-comercial

# Webhook de campañas en n8n (Valeria)
N8N_CAMPAIGN_WEBHOOK=https://ocean-n8n.hj1tdk.easypanel.host/webhook/campana-masiva

# CORS: el dominio del frontend
CORS_ORIGIN=https://crm.oceanindustries.com.co

# Frontend: apunta al mismo dominio (las funciones /api viven en el mismo proyecto)
VITE_API_URL=https://crm.oceanindustries.com.co
